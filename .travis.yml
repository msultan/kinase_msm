language: python
python:
  - "2.6"
  - "2.7"
  - "3.3"
  - "3.4"

branches:
  only:
    - master
install:
  - source devtools/ci/install.sh

script:
  - nosetests -v
  - flake8 `find . -name '*.py'  ! -name build ! -path './build/*' !
            -path './docs*' | xargs`
